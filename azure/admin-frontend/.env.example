VITE_API_URL=https://vms-apim.azure-api.net/api
VITE_B2C_TENANT=vmsuniversity
VITE_CLIENT_ID=<admin-spa-client-id>          # this portal's own SPA app registration ID
VITE_API_SCOPE=api://<api-client-id>/access   # the backend API app's exposed scope (same value in both portals)
